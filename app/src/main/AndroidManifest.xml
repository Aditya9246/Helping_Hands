<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/volunteer_logo_blue"
        android:label="Helping Hands"
        android:roundIcon="@drawable/volunteer_logo_blue"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestApp"
        tools:targetApi="31">
        <activity
            android:name=".ChannelActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mainPage" />
        <activity android:name=".ProfilePage" />
        <activity android:name=".SettingsPage" />
        <activity android:name=".ProfilePageAdmin" />
        <activity android:name=".CreateEvent" />
        <activity android:name=".EventSignUp" />
        <activity android:name=".StreamChat" />
    </application>

</manifest>